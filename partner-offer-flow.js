const ENTEGO_PACKAGES_KEY='entego_partner_packages',ENTEGO_AVAIL_KEY='entego_partner_availability',ENTEGO_SELECTED_PACKAGE='entego_selected_package';
const ENTEGO_SELECTED_PACKAGES_KEY='entego_selected_partner_packages',ENTEGO_SELECTED_AVAIL_KEY='entego_selected_partner_availability',ENTEGO_SELECTED_PROFILE_KEY='entego_selected_partner_profile';
const offerRead=(key,fallback)=>{try{return JSON.parse(localStorage.getItem(key)||JSON.stringify(fallback))}catch{return fallback}};
const offerSave=(key,value)=>localStorage.setItem(key,JSON.stringify(value));
const offerEsc=value=>String(value??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
const offerMoney=value=>`Rp${Number(value||0).toLocaleString('id-ID')}`;
const offerRoute=()=>localStorage.getItem('entego_route')||'home';
const customerOfferContext=()=>['detail','booking','checkout'].includes(offerRoute())&&Boolean(localStorage.getItem('entego_partner_user_id'));
function getPackages(){return offerRead(customerOfferContext()?ENTEGO_SELECTED_PACKAGES_KEY:ENTEGO_PACKAGES_KEY,[]).filter(x=>x&&x.id&&x.name&&Number(x.price)>0)}
const getAvailability=()=>offerRead(customerOfferContext()?ENTEGO_SELECTED_AVAIL_KEY:ENTEGO_AVAIL_KEY,[]).filter(x=>x&&x.date&&x.status);
const selectedPackage=()=>{const items=getPackages(),id=localStorage.getItem(ENTEGO_SELECTED_PACKAGE);return items.find(x=>x.id===id)||items[0]||null};
function packageManager(){if(offerRoute()!=='partnerPackages')return;const main=document.querySelector('main.content');if(!main||main.dataset.entegoPackagesReady==='1')return;main.dataset.entegoPackagesReady='1';let editing='';const draw=()=>{const items=getPackages(),current=items.find(x=>x.id===editing)||{};main.innerHTML=`<div class="kicker">PAKET & HARGA</div><h2>Kelola penawaran</h2><p class="meta">Hanya paket yang benar-benar kamu buat yang dapat tampil ke Customer. ENTEGO tidak membuat harga contoh otomatis.</p><div class="card"><div class="field"><label>Nama paket</label><input id="pkgName" value="${offerEsc(current.name||'')}" placeholder="Contoh: Wedding DJ 3 Jam"></div><div class="field"><label>Durasi</label><input id="pkgDuration" value="${offerEsc(current.duration||'')}" placeholder="3 jam / Full day"></div><div class="field"><label>Harga</label><input id="pkgPrice" inputmode="numeric" value="${offerEsc(current.price||'')}" placeholder="Masukkan harga sebenarnya"></div><div class="field"><label>Deskripsi</label><textarea id="pkgDesc" rows="3">${offerEsc(current.description||'')}</textarea></div><button class="btn primary" id="savePackage" style="width:100%">${editing?'Simpan Perubahan':'+ Tambah Paket'}</button>${editing?'<button class="btn soft" id="cancelPackageEdit" style="width:100%;margin-top:8px">Batal Edit</button>':''}</div>${items.length?items.map(item=>`<div class="card"><div class="row between"><div><span class="pill ${item.featured?'green':''}">${item.featured?'★ Paket Utama':'Paket'}</span><h3>${offerEsc(item.name)}</h3><div class="meta">${offerEsc(item.duration)} • ${offerMoney(item.price)}</div></div><div class="price">${offerMoney(item.price)}</div></div><p class="meta">${offerEsc(item.description||'')}</p><div class="row" style="gap:8px"><button class="btn soft mini" data-edit-package="${offerEsc(item.id)}" style="flex:1">Edit</button><button class="btn soft mini" data-feature-package="${offerEsc(item.id)}" style="flex:1">${item.featured?'Utama ✓':'Jadi Utama'}</button><button class="btn soft mini" data-delete-package="${offerEsc(item.id)}">Hapus</button></div></div>`).join(''):'<div class="card"><b>Belum ada paket.</b><p class="meta">Tambahkan paket dan harga sebenarnya sebelum profil dapat menerima booking.</p></div>'}`;main.querySelector('#savePackage').onclick=()=>{const name=main.querySelector('#pkgName').value.trim(),duration=main.querySelector('#pkgDuration').value.trim(),price=Number(main.querySelector('#pkgPrice').value.replace(/\D/g,'')),description=main.querySelector('#pkgDesc').value.trim();if(!name||!duration||!price)return alert('Nama paket, durasi, dan harga wajib diisi.');let next=getPackages();if(editing)next=next.map(x=>x.id===editing?{...x,name,duration,price,description}:x);else next.push({id:`pkg-${Date.now()}`,name,duration,price,description,featured:next.length===0});offerSave(ENTEGO_PACKAGES_KEY,next);editing='';draw()};main.querySelector('#cancelPackageEdit')?.addEventListener('click',()=>{editing='';draw()});main.querySelectorAll('[data-edit-package]').forEach(b=>b.onclick=()=>{editing=b.dataset.editPackage;draw();scrollTo(0,0)});main.querySelectorAll('[data-feature-package]').forEach(b=>b.onclick=()=>{offerSave(ENTEGO_PACKAGES_KEY,getPackages().map(x=>({...x,featured:x.id===b.dataset.featurePackage})));draw()});main.querySelectorAll('[data-delete-package]').forEach(b=>b.onclick=()=>{offerSave(ENTEGO_PACKAGES_KEY,getPackages().filter(x=>x.id!==b.dataset.deletePackage));draw()})};draw()}
function availabilityManager(){if(offerRoute()!=='partnerCalendar')return;const main=document.querySelector('main.content');if(!main||main.dataset.entegoAvailabilityReady==='1')return;main.dataset.entegoAvailabilityReady='1';const draw=()=>{const slots=getAvailability().sort((a,b)=>a.date.localeCompare(b.date));main.innerHTML=`<div class="kicker">AVAILABILITY</div><h2>Kalender & ketersediaan</h2><p class="meta">Tandai tanggal yang memang sibuk atau tidak tersedia. Tidak ada tanggal contoh yang dibuat ENTEGO.</p><div class="card"><div class="field"><label>Tanggal</label><input id="availDate" type="date"></div><div class="field"><label>Status</label><select id="availStatus"><option value="unavailable">Tidak tersedia</option><option value="booked">Sudah dibooking</option><option value="available">Tersedia</option></select></div><div class="field"><label>Catatan</label><input id="availNote"></div><button class="btn primary" id="saveAvailability" style="width:100%">Simpan Tanggal</button></div>${slots.length?slots.map(s=>`<div class="card row between"><div><b>${new Date(s.date+'T00:00:00').toLocaleDateString('id-ID',{day:'numeric',month:'long',year:'numeric'})}</b><div class="meta">${offerEsc(s.note||'Tanpa catatan')}</div></div><div><span class="pill ${s.status==='available'?'green':'blue'}">${s.status==='available'?'Tersedia':s.status==='booked'?'Booked':'Tidak tersedia'}</span><br><button class="link" data-remove-availability="${offerEsc(s.date)}">Hapus</button></div></div>`).join(''):'<div class="card"><b>Belum ada pengecualian jadwal.</b></div>'}`;main.querySelector('#saveAvailability').onclick=()=>{const date=main.querySelector('#availDate').value,status=main.querySelector('#availStatus').value,note=main.querySelector('#availNote').value.trim();if(!date)return alert('Pilih tanggal terlebih dahulu.');const next=getAvailability().filter(x=>x.date!==date);next.push({date,status,note});offerSave(ENTEGO_AVAIL_KEY,next);draw()};main.querySelectorAll('[data-remove-availability]').forEach(b=>b.onclick=()=>{offerSave(ENTEGO_AVAIL_KEY,getAvailability().filter(x=>x.date!==b.dataset.removeAvailability));draw()})};draw()}
function publicPackages(){if(offerRoute()!=='detail'||!localStorage.getItem('entego_partner_user_id'))return;const id=localStorage.getItem('entego_partner_user_id'),stamp=sessionStorage.getItem('entego_partner_hydrated')||'';if(!stamp.startsWith(id+':'))return;const main=document.querySelector('main.content');if(!main)return;const section=[...main.querySelectorAll('section.section')].find(s=>s.querySelector('h2')?.textContent.includes('Paket populer')||s.querySelector('h2')?.textContent.includes('Paket & Harga'));if(!section||section.dataset.entegoOfferReady==='1')return;section.dataset.entegoOfferReady='1';const items=getPackages(),bookingBtn=document.querySelector('.sticky-cta [data-route="booking"]');if(!items.length){section.innerHTML='<h2>Paket & Harga</h2><div class="card"><b>Belum ada paket aktif.</b></div>';if(bookingBtn){bookingBtn.disabled=true;bookingBtn.textContent='Belum dapat dibooking'}return}section.innerHTML=`<h2>Paket & Harga</h2>${items.map(item=>`<div class="card"><div class="row between"><div><span class="pill ${item.featured?'green':''}">${item.featured?'★ Utama':'Paket'}</span><h3>${offerEsc(item.name)}</h3><div class="meta">${offerEsc(item.duration)}</div></div><div class="price">${offerMoney(item.price)}</div></div><p class="meta">${offerEsc(item.description||'')}</p><button class="btn ${item.featured?'primary':'soft'}" data-select-package="${offerEsc(item.id)}" style="width:100%">Pilih Paket</button></div>`).join('')}`;section.querySelectorAll('[data-select-package]').forEach(b=>b.onclick=()=>{localStorage.setItem(ENTEGO_SELECTED_PACKAGE,b.dataset.selectPackage);localStorage.setItem('entego_route','booking');location.reload()})}
function bookingAvailability(){if(offerRoute()!=='booking')return;const main=document.querySelector('main.content');if(!main)return;const pkg=selectedPackage(),next=document.querySelector('.sticky-cta [data-route="checkout"]');if(!pkg){if(next){next.disabled=true;next.textContent='Paket belum tersedia'}return}if(!document.querySelector('#entegoSelectedPackage')){const card=document.createElement('section');card.id='entegoSelectedPackage';card.className='card';card.innerHTML=`<div class="kicker">PAKET DIPILIH</div><div class="row between"><div><b>${offerEsc(pkg.name)}</b><div class="meta">${offerEsc(pkg.duration)}</div></div><div class="price">${offerMoney(pkg.price)}</div></div>`;main.prepend(card)}const input=main.querySelector('input[type="date"]');if(!input||input.dataset.entegoAvailability==='1')return;input.dataset.entegoAvailability='1';const box=document.createElement('div');box.id='entegoAvailabilityStatus';box.className='card';input.closest('.field')?.after(box);const check=()=>{const slot=getAvailability().find(x=>x.date===input.value),blocked=slot&&slot.status!=='available';box.innerHTML=blocked?`<b>⛔ Tanggal tidak tersedia</b><div class="meta">${offerEsc(slot.note||'Mitra tidak menerima booking pada tanggal ini.')}</div>`:'<b>✓ Validasi akhir di server</b><div class="meta">Availability dan booking aktif diperiksa ulang saat order dibuat.</div>';if(next)next.disabled=!!blocked};input.addEventListener('change',check);check()}
function checkoutPackage(){if(offerRoute()!=='checkout')return;const pkg=selectedPackage(),main=document.querySelector('main.content');if(!main||main.dataset.entegoCheckoutPackage==='1')return;main.dataset.entegoCheckoutPackage='1';const pay=document.querySelector('#payBtn');if(!pkg){if(pay){pay.disabled=true;pay.textContent='Paket tidak tersedia'}return}const first=main.querySelector('.card');if(!first)return;const h3=first.querySelector('h3');if(h3)h3.textContent=`${profileName()} — ${pkg.name}`;const rows=[...first.querySelectorAll('.row.between')],service=rows.find(r=>r.firstElementChild?.textContent.includes('Harga layanan')),promo=rows.find(r=>r.firstElementChild?.textContent.toLowerCase().includes('promo')),total=rows.find(r=>r.firstElementChild?.textContent.trim()==='Total');if(service)service.lastElementChild.textContent=offerMoney(pkg.price);if(promo){promo.firstElementChild.textContent='Promo';promo.lastElementChild.textContent=offerMoney(0)}if(total)total.lastElementChild.textContent=offerMoney(Number(pkg.price)+75000)}
function profileName(){return offerRead(ENTEGO_SELECTED_PROFILE_KEY,{}).displayName||'Mitra ENTEGO'}
const offerRun=()=>{packageManager();availabilityManager();publicPackages();bookingAvailability();checkoutPackage()};new MutationObserver(offerRun).observe(document.documentElement,{childList:true,subtree:true});window.addEventListener('load',offerRun);offerRun();
