const ENTEGO_PROFILE_KEY='entego_partner_profile';
const ENTEGO_PORTFOLIO_KEY='entego_partner_portfolio';
const ENTEGO_SELECTED_PROFILE_KEY='entego_selected_partner_profile';
const ENTEGO_SELECTED_PORTFOLIO_KEY='entego_selected_partner_portfolio';

const readJSON=(key,fallback)=>{try{return JSON.parse(localStorage.getItem(key)||JSON.stringify(fallback))}catch{return fallback}};
const saveJSON=(key,value)=>localStorage.setItem(key,JSON.stringify(value));
const route=()=>localStorage.getItem('entego_route')||'home';
const usingSelected=()=>route()==='detail'&&Boolean(localStorage.getItem('entego_partner_user_id'));
const profile=()=>readJSON(usingSelected()?ENTEGO_SELECTED_PROFILE_KEY:ENTEGO_PROFILE_KEY,{});
const portfolio=()=>readJSON(usingSelected()?ENTEGO_SELECTED_PORTFOLIO_KEY:ENTEGO_PORTFOLIO_KEY,[]);
const ownProfile=()=>readJSON(ENTEGO_PROFILE_KEY,{});
const ownPortfolio=()=>readJSON(ENTEGO_PORTFOLIO_KEY,[]);
const esc=value=>String(value??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
const money=value=>`Rp${Number(value||0).toLocaleString('id-ID')}`;

function clearSelected(){['entego_selected_partner_profile','entego_selected_partner_packages','entego_selected_partner_availability','entego_selected_partner_portfolio','entego_selected_package'].forEach(k=>localStorage.removeItem(k));sessionStorage.removeItem('entego_partner_hydrated')}
function openPublicProfile(){const u=readJSON('entego_auth_user',null);if(u?.role!=='partner'||!u.id){alert('Login sebagai Mitra diperlukan untuk melihat profil publik.');return}clearSelected();localStorage.setItem('entego_partner_user_id',u.id);localStorage.setItem('entego_route','detail');location.reload()}

function enhancePartnerDashboard(){
 if(route()!=='partner')return;const main=document.querySelector('main.content');if(!main)return;const p=ownProfile();let card=document.querySelector('#partnerPublicPreview');if(!card){card=document.createElement('section');card.id='partnerPublicPreview';card.className='card';main.prepend(card)}if(card.dataset.marketplaceReady==='1')return;card.dataset.marketplaceReady='1';const items=ownPortfolio(),cover=p.cover||items[0]?.src||'';card.innerHTML=`${cover?`<img src="${esc(cover)}" alt="Cover profil" style="width:100%;height:180px;object-fit:cover;border-radius:16px;margin-bottom:14px">`:''}<div class="kicker">PROFIL PUBLIK MITRA</div><h2>${esc(p.displayName||'Profil Mitra ENTEGO')}</h2><div class="meta">${esc(p.specialty||'Entertainment & Event Partner')} • ${esc(p.area||'Bali')}</div>${p.price?`<p><b>Mulai ${money(p.price)}</b></p>`:''}<p>${esc(p.bio||'Lengkapi profil profesional agar pelanggan lebih mudah mengenal layanan kamu.')}</p><div class="row" style="gap:8px;flex-wrap:wrap"><span class="pill">🖼️ ${items.length} portfolio</span></div><div class="row" style="gap:10px;margin-top:14px"><button class="btn soft" id="managePortfolioBtn" style="flex:1">Kelola Portfolio</button><button class="btn primary" id="viewPublicProfileBtn" style="flex:1">Lihat Profil Publik</button></div>`;card.querySelector('#managePortfolioBtn').onclick=()=>{localStorage.setItem('entego_route','partnerPortfolio');location.reload()};card.querySelector('#viewPublicProfileBtn').onclick=openPublicProfile;
}

function imageToDataURL(file){return new Promise((resolve,reject)=>{const reader=new FileReader();reader.onerror=reject;reader.onload=()=>{const img=new Image();img.onerror=reject;img.onload=()=>{const max=900,scale=Math.min(1,max/Math.max(img.width,img.height)),canvas=document.createElement('canvas');canvas.width=Math.max(1,Math.round(img.width*scale));canvas.height=Math.max(1,Math.round(img.height*scale));canvas.getContext('2d').drawImage(img,0,0,canvas.width,canvas.height);resolve(canvas.toDataURL('image/jpeg',0.72))};img.src=reader.result};reader.readAsDataURL(file)})}

function renderPortfolioManager(){
 if(route()!=='partnerPortfolio')return;const main=document.querySelector('main.content');if(!main||main.dataset.entegoPortfolioReady==='1')return;main.dataset.entegoPortfolioReady='1';
 const draw=()=>{const items=ownPortfolio(),p=ownProfile();main.innerHTML=`<div class="kicker">PORTFOLIO MITRA</div><h2>Foto & karya terbaik</h2><p class="meta">Tambahkan foto dari HP atau URL. Portfolio server akan menjadi sumber profil publik.</p><div class="card"><div class="field"><label>Judul karya/event</label><input id="portfolioTitle" placeholder="Contoh: Wedding at Seminyak"></div><div class="field"><label>Tambah dari HP</label><input id="portfolioFiles" type="file" accept="image/*" multiple></div><div class="field"><label>Atau URL foto</label><input id="portfolioUrl" inputmode="url" placeholder="https://..."></div><button class="btn primary" id="addPortfolioUrl" style="width:100%">+ Tambah Portfolio</button></div><div class="section-head"><h2>Portfolio (${items.length}/12)</h2><button class="link" id="previewPublicPortfolio">Lihat publik</button></div>${items.length?`<div style="display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px">${items.map((item,i)=>`<article class="card" style="padding:8px;margin:0"><img src="${esc(item.src)}" alt="${esc(item.title||'Portfolio')}" style="width:100%;aspect-ratio:1/1;object-fit:cover;border-radius:12px"><b style="display:block;margin:9px 2px 3px">${esc(item.title||`Portfolio ${i+1}`)}</b><div class="row" style="gap:6px"><button class="btn soft mini" data-cover="${esc(item.id)}" style="flex:1">${p.cover===item.src?'✓ Cover':'Jadi Cover'}</button><button class="btn soft mini" data-remove="${esc(item.id)}">Hapus</button></div></article>`).join('')}</div>`:`<div class="card"><b>Belum ada foto portfolio.</b><p class="meta">Tambahkan karya terbaik agar profil publik lebih meyakinkan.</p></div>`}`;
 const addItem=(src,title)=>{const current=ownPortfolio();if(current.length>=12){alert('Maksimal 12 foto portfolio untuk versi ini.');return}current.unshift({id:`pf-${Date.now()}-${Math.random().toString(16).slice(2)}`,title:(title||'Portfolio ENTEGO').trim(),src});try{saveJSON(ENTEGO_PORTFOLIO_KEY,current)}catch{alert('Penyimpanan foto di perangkat penuh. Hapus beberapa foto lalu coba lagi.');return}const next=ownProfile();if(!next.cover){next.cover=src;saveJSON(ENTEGO_PROFILE_KEY,next)}draw()};
 main.querySelector('#addPortfolioUrl').onclick=()=>{const url=main.querySelector('#portfolioUrl').value.trim(),title=main.querySelector('#portfolioTitle').value.trim();if(!/^https?:\/\//i.test(url)){alert('Masukkan URL foto yang valid.');return}addItem(url,title)};main.querySelector('#portfolioFiles').onchange=async e=>{const files=[...e.target.files].filter(f=>f.type.startsWith('image/')),title=main.querySelector('#portfolioTitle').value.trim();for(const file of files.slice(0,Math.max(0,12-ownPortfolio().length))){try{addItem(await imageToDataURL(file),title||file.name.replace(/\.[^.]+$/,''))}catch{alert(`Foto ${file.name} gagal diproses.`)}}};main.querySelector('#previewPublicPortfolio').onclick=openPublicProfile;main.querySelectorAll('[data-remove]').forEach(btn=>btn.onclick=()=>{const current=ownPortfolio(),target=current.find(x=>x.id===btn.dataset.remove);saveJSON(ENTEGO_PORTFOLIO_KEY,current.filter(x=>x.id!==btn.dataset.remove));const next=ownProfile();if(target&&next.cover===target.src){next.cover=ownPortfolio()[0]?.src||'';saveJSON(ENTEGO_PROFILE_KEY,next)}draw()});main.querySelectorAll('[data-cover]').forEach(btn=>btn.onclick=()=>{const item=ownPortfolio().find(x=>x.id===btn.dataset.cover);if(!item)return;const next=ownProfile();next.cover=item.src;saveJSON(ENTEGO_PROFILE_KEY,next);draw()})};draw();
}

function enrichPublicDetail(){
 if(route()!=='detail'||!localStorage.getItem('entego_partner_user_id'))return;const main=document.querySelector('main.content');if(!main||document.querySelector('#entegoPartnerPublicExtra'))return;const p=profile(),items=portfolio(),stamp=sessionStorage.getItem('entego_partner_hydrated')||'',id=localStorage.getItem('entego_partner_user_id');if(!p.displayName||!stamp.startsWith(id+':'))return;const title=document.querySelector('.page-title span');if(title)title.textContent=p.displayName;const extra=document.createElement('section');extra.id='entegoPartnerPublicExtra';extra.className='section';extra.innerHTML=`${p.cover?`<img src="${esc(p.cover)}" alt="${esc(p.displayName)}" style="width:100%;height:220px;object-fit:cover;border-radius:18px;margin-bottom:14px">`:''}<div class="kicker">PROFIL PUBLIK</div><h2>${esc(p.displayName)}</h2><div class="meta">${esc(p.specialty||'Entertainment & Event Partner')} • ${esc(p.area||'Bali')}</div><p>${esc(p.bio||'Mitra profesional ENTEGO.')}</p>${p.social?`<span class="pill">📷 ${esc(p.social)}</span>`:''}${items.length?`<h3 style="margin-top:18px">Portfolio</h3><div style="display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px">${items.slice(0,6).map(item=>`<img src="${esc(item.src)}" alt="${esc(item.title||'Portfolio')}" style="width:100%;aspect-ratio:1/1;object-fit:cover;border-radius:12px">`).join('')}</div>`:''}`;const first=main.querySelector(':scope > .section');if(first)first.after(extra);else main.prepend(extra);
}

const run=()=>{enhancePartnerDashboard();renderPortfolioManager();enrichPublicDetail()};new MutationObserver(run).observe(document.documentElement,{childList:true,subtree:true});window.addEventListener('load',run);run();
