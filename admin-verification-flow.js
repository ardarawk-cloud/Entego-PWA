const avRoute=()=>localStorage.getItem('entego_route')||'home';
const avEsc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
async function avJson(url,options){const r=await fetch(url,{cache:'no-store',headers:{accept:'application/json',...(options?.headers||{})},...options});let d={};try{d=await r.json()}catch{};return {r,d}}
function avStatus(p,a){if(a?.verified||p?.verificationStatus==='approved')return ['green','✓ Terverifikasi'];if(p?.verificationStatus==='rejected')return ['blue','Ditolak'];return ['blue','Menunggu review']}
async function avRender(){if(avRoute()!=='adminVerify')return;const main=document.querySelector('main.content');if(!main||main.dataset.entegoVerificationReady==='1')return;main.dataset.entegoVerificationReady='1';main.innerHTML='<div class="kicker">PARTNER VERIFICATION</div><h2>Verifikasi Mitra</h2><div class="card"><span class="pill">Memuat data server…</span></div>';try{const x=await avJson('/api/admin/partners');if(!x.r.ok||!x.d.ok)throw new Error(x.d.error||'load_failed');const rows=x.d.partners||[];main.innerHTML=`<div class="kicker">PARTNER VERIFICATION</div><div class="row between"><h2>Verifikasi Mitra</h2><span class="pill green">${rows.length} akun</span></div>${rows.length?rows.map(({account,profile})=>{const st=avStatus(profile,account);return `<article class="card" data-av-user="${avEsc(account.id)}"><div class="row between"><div><b>${avEsc(profile?.displayName||account.displayName)}</b><div class="meta">${avEsc(account.email)}</div></div><span class="pill ${st[0]}">${st[1]}</span></div><div class="divider"></div><div class="meta">${avEsc(profile?.category||'Belum pilih kategori')} • ${avEsc(profile?.area||'Area belum diisi')}</div><p class="meta">${avEsc(profile?.bio||'Profil profesional belum lengkap.')}</p>${profile?.verificationNote?`<div class="meta">Catatan admin: ${avEsc(profile.verificationNote)}</div>`:''}<div class="row" style="gap:8px;margin-top:12px"><button class="btn soft av-reject" style="flex:1">Tolak</button><button class="btn soft av-reset" style="flex:1">Reset</button><button class="btn primary av-approve" style="flex:1">Verifikasi</button></div></article>`}).join(''):'<div class="card"><b>Belum ada akun Mitra.</b></div>'}`;main.querySelectorAll('[data-av-user]').forEach(card=>{const uid=card.dataset.avUser;card.querySelector('.av-approve').onclick=()=>avAct(uid,'approve','');card.querySelector('.av-reject').onclick=()=>{const note=prompt('Alasan penolakan verifikasi:')||'';if(note.trim())avAct(uid,'reject',note)};card.querySelector('.av-reset').onclick=()=>avAct(uid,'reset','Menunggu pemeriksaan ulang')})}catch(e){main.innerHTML=`<div class="kicker">PARTNER VERIFICATION</div><h2>Verifikasi Mitra</h2><div class="card"><span class="pill blue">Tidak dapat dimuat</span><p class="meta">${e.message==='admin_required'?'Login dengan akun Admin diperlukan.':'Backend verifikasi belum tersedia di deployment aktif.'}</p></div>`}}
async function avAct(uid,action,note){const cards=[...document.querySelectorAll('[data-av-user]')],card=cards.find(x=>x.dataset.avUser===uid);card?.querySelectorAll('button').forEach(b=>b.disabled=true);try{const x=await avJson(`/api/admin/partners/${encodeURIComponent(uid)}/verification`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({action,note})});if(!x.r.ok||!x.d.ok)throw new Error(x.d.error||'verify_failed');const main=document.querySelector('main.content');if(main)delete main.dataset.entegoVerificationReady;avRender()}catch(e){card?.querySelectorAll('button').forEach(b=>b.disabled=false);alert(e.message==='partner_profile_required'?'Mitra harus melengkapi profil sebelum diverifikasi.':'Status verifikasi belum dapat disimpan.')}}
const avRun=()=>avRender();new MutationObserver(avRun).observe(document.documentElement,{childList:true,subtree:true});window.addEventListener('load',avRun);avRun();
